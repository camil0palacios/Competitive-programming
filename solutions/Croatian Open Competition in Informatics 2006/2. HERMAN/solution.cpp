#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

const double pi = acos(-1);

int main(){
    int r;
    cin >> r;
    cout << fixed << setprecision(10) << pi*r*r << endl;
    cout << setprecision(10) << 2*r*r << endl;
    return 0;   
}